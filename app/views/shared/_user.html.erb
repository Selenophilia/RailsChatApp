<div class="mb-3">
    <h1 class="display-4"> Inbox </h1>
      <nav class="nav flex-column">
        <% @users.each do |user| %>
                  <%= link_to user.username , create_conversation_messages_path(sender_id: current_user.id, recipient_id: user.id),class: "nav-link room-nav-link", method: :post%>
        <% end %>
      </nav>
</div>